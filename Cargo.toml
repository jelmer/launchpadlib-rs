[package]
name = "launchpadlib"
version = "0.1.2"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/jelmer/launchpadlib-rs"
readme = "README.md"
description = "Rust library for accessing Launchpad.net"
keywords = ["launchpad", "api", "rust"]
categories = ["network-programming", "asynchronous", "api-bindings"]
authors = ["Jelmer Vernooĳ <jelmer@jelmer.uk>"]
build = "build.rs"
include = ["wadl/*.wadl", "src/*.rs", "build.rs"]

[lib]

[workspace.dependencies]
#wadl = { version = "0.1.4", default-features = false }
wadl = { path = "../wadl-rs" }

[dependencies]
chrono = { version = "0.4.31", features = ["serde"] }
debversion = { version = "0.2.2", features = ["serde"] }
hmac = "0.12.1"
lazy_static = "1.4.0"
rand = "0.8.5"
reqwest = { version = "0.11.22", features = ["json", "blocking"] }
serde = { version = "1.0.189", features = ["derive"] }
serde_json = "1.0.107"
sha1 = "0.10.6"
url = { version = "2.4.1", features = ["serde"] }
wadl = { workspace = true }

[build-dependencies]
wadl = { workspace = true, features = ["codegen"] }
reqwest = { version = "0.11.22", features = ["blocking"] }

[features]
default = ["api-v1_0"]
api-v1_0 = []
api-devel = []
api-beta = []
